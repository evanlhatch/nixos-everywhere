# config/nixos.env
# NixOS specific default configurations, primarily for nixos-everywhere.sh.
# These can be overridden by .env or justfile command-line arguments.

# Default Nix Flake URI (if not specified in `just provision`)
# It's usually better to provide the full flake_uri (including host attribute) directly.
# NIXOS_DEFAULT_FLAKE_URI="github:NixOS/nixpkgs"

# Default NixOS host attribute within the Flake (if not specified in `just provision`)
# NIXOS_DEFAULT_TARGET_HOST_ATTR="nixosConfigurations.default" # Example

# Default NixOS channel for the bootstrap phase of nixos-everywhere.sh
NIXOS_DEFAULT_NIXOS_CHANNEL_ENV="nixos-24.05" # Or nixos-unstable, etc.

# Default initial hostname for the OS (before NixOS takes over if it defines its own)
NIXOS_DEFAULT_HOSTNAME_INIT_ENV="nixos-server"

# Default timezone for the NixOS system
NIXOS_DEFAULT_TIMEZONE_INIT_ENV="Etc/UTC"

# Default locale for the NixOS system
NIXOS_DEFAULT_LOCALE_LANG_INIT_ENV="en_US.UTF-8"

# Default system.stateVersion for NixOS
NIXOS_DEFAULT_STATE_VERSION_INIT_ENV="24.05"

# Default SSH user for connecting to the final NixOS system
# (Can also be set in common.env or .env)
# NIXOS_SSH_USER="root"

# URL to fetch nixos-everywhere.sh from
# IMPORTANT: This URL must point to a publicly accessible location where the nixos-everywhere.sh script is hosted.
# If you make changes to the script locally, make sure to push those changes to this location.
NIXOS_EVERYWHERE_SCRIPT_URL="https://raw.githubusercontent.com/evanlhatch/nixos-everywhere/main/scripts/nixos_everywhere.sh"